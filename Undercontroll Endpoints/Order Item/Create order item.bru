meta {
  name: Create order item
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/v1/api/order-items
  body: json
  auth: inherit
}

body:json {
  {
    
  }
}

settings {
  encodeUrl: true
}
