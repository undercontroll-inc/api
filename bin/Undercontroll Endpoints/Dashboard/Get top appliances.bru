meta {
  name: Get top appliances
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/api/dashboard/charts/top-appliances?period=ALL&status=ALL
  body: none
  auth: bearer
}

params:query {
  period: ALL
  status: ALL
}

auth:bearer {
  token: {{token}}
}
