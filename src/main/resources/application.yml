spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: "umaChaveSuperSeguraComMaisDe32Caracteres"
  application:
      name: api
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:undercontroll-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
