spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          # Alterar essa chave em prod, exemplo apenas para desenvolvimento
          secret-key: "Tj1E-AbULqQy-YJioSkDefRonxhKz0x3MqlUoRObwe8="
          public-key-location:application:
      name: api
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:undercontroll-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;NON_KEYWORDS=USER,ORDER
    driver-class-name: org.h2.Driver
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        globally_quoted_identifiers: true

firebase:
  config:
    path: firebase-service-account.json

server:
  error:
    include-binding-errors: always
    include-message: always
