spring:
  mail:
    host: localhost
    port: 25
    username: test
    password: test
  cache:
    type: simple
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
logging:
  level:
    root: ERROR

